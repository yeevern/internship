{
  "name": "nanikore-docker",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build-image": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "",
        "command": "docker build $(cat apps/nanikore/docker/env.list | while read line; do echo \"--build-arg $line\"; done;) -f apps/nanikore/docker/dockerfile -t nanikore-website ."
      }
    }
  }
}
